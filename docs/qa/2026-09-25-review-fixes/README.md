# 2026-09-25 提交复核后的修正

用户要求修正复核意见，在main提交，并推送到指定的Jexceed/playground-history仓库。基线为6c6160d。现有13项本机临时指针/缓存保持，不纳入本次提交。

## 改动与边界

- ObjectWorkbench的scene-find身份标签移到图下；热区和图片仍处于原共同坐标层。删除按窄屏提层的补丁，不改变章节内容、图片文件、语音或内容版本。
- 完整103章/515步中共有30章使用scene-find（30步），其中3步为实物照片；共享修正覆盖这些步骤，隐藏69章没有新增该玩法，也不据此标为新版完成。
- viewport-audit检查按钮完整边界、祖先裁切、中心与四角命中、页面是否已滚动。sweep在选项/热点反馈、模型操作后检查；下一步点击前与结束页强制要求主按钮存在，异常停止该章并使最终退出码非零。
- 巡检只自动检查当前找图的可见身份标签，避免把折叠亲子区标签或透明装饰层误判为视觉遮挡；其他图面仍须人工目验。
- 清理两处未使用变量警告；为巡检边界与错误状态增加4项测试。旧verify-fixes是历史定点复现脚本，本轮未执行其历史尺寸矩阵。

## 实际验证

- 本机服务HTTP 200，实际URL为http://127.0.0.1:4173/。开始无已打开的浏览器标签，新建检查页并实测1280×720，无viewport覆盖；这是本轮唯一目标尺寸。
- 开元通宝五步走完：实物照片标签与两侧热点不重叠；右面错选后能改选左面，回应与下一步正常。第一步及第4步答对后按钮底边646.80px，找图答对后577.55px；下一次点击前测量，scrollY=0，多点命中按钮。
- 开元结束页两个主按钮底边681.40px，均完整可见；“再玩本章”返回本章封面。
- 李白章：时间题、故事页、找图页；故事插画标签置于图外且不覆盖人物热点。答对后的下一步底边577.55px，scrollY=0，多点命中按钮。
- 上述页面无横向溢出、无坏图。实际截图在工具中查看，未新写图像素材。
- `npm run check:local`通过：严格素材、完整内容打包/审计、构建、57/57测试、lint无警告、类型检查。日志见`check.log`。
- 新巡检测试覆盖可见按钮、未作答时无按钮、完成后导航缺失、顶部可见但底部越界、遮挡、祖先裁切、隐藏，以及自动滚动后的假通过。巡检脚本语法检查通过。

## 未覆盖

本轮没有重跑34章全量浏览器通关，旧sweep-after-fix.log不能作为新增可见性断言的通过证明。没有人耳逐条校音、真实儿童验证或史实终审。目标是修正这两项界面/验收问题，不代表内容冻结或公网试玩发布。

后续全量巡检先测当轮实际窗口，再显式传尺寸，例如`node docs/qa/2026-09-24-experience-review/shots/sweep.mjs 1280x720`；不追加多端矩阵。脚本使用历史记录中的playwright-core临时依赖，本轮浏览器操作通过现有浏览器工具完成。
