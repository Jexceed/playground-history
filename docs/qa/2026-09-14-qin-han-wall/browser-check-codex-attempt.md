# 秦朝长城任务浏览器实测记录

- 记录时间：2026-09-14 16:04（Asia/Shanghai）。
- 唯一允许访问的页面：`http://127.0.0.1:4173/`。
- 目标任务：第4站“秦朝” → “帮长城连上旧墙段”。
- 要求尺寸：手机 390×600；桌面 1280×800。
- 本轮结果：**实测未完成，浏览器连接与授权阻塞。A—G 均未验证。**

本报告记录本轮实际尝试，不沿用旧截图或先前 QA 结论。未取得当前页面、DOM、截图或控制台日志，不能将未验证项目判定为通过，也不能据此断言游戏功能失败。

## 环境检查与阻塞证据

| 编号 | 实际操作 | 工具返回与可得结论 |
| --- | --- | --- |
| ENV-01 | `lsof -nP -iTCP:4173 -sTCP:LISTEN` | `node`，PID `1576`，监听 `127.0.0.1:4173`。只证明存在监听进程，不证明网页正常响应或是当前版本。 |
| ENV-02 | 从终端向唯一指定地址发送一次 HTTP 检查 | `curl` 退出码 `7`，HTTP 状态输出 `000`；当前终端环境未能建立连接。不能将 `000` 当成服务器返回的 HTTP 状态码。未更换地址或端口。 |
| ENV-03 | 用 `cua.getBrowser({ url: "http://127.0.0.1:4173/" })` 选择浏览器 | 工具选择 Chrome 扩展通道，浏览器 ID `1`，返回控制文档；尚未打开或取得页面。 |
| ENV-04 | 设置测试会话名称并尝试列出可复用标签 | 约30秒后 `js execution timed out; kernel reset, rerun your request`，未取得标签列表。 |
| ENV-05 | 恢复同一个 Chrome 通道，读取工具提供的故障恢复、截图、尺寸与本机测试文档；单独重试设置会话名称 | 文档读取成功；会话设置约30秒后返回 `nodeRepl.fetch request failed`。 |
| ENV-06 | `cua.getState()` 检查可用控制通道 | 再次约30秒超时并重置，未取得应用/标签状态。 |
| ENV-07 | `cua.getApp("com.google.Chrome")` 尝试直接连接本机 Chrome | `Computer Use was not approved to use Google Chrome`。这是工具授权拒绝；未绕过该限制。 |
| ENV-08 | 用工具提供的内置浏览器入口打开同一本机地址 | `Browser is not available: iab`；没有可用内置浏览器。 |

准备期间还尝试过 Node REPL 自动化依赖入口，返回模块导出错误，未启动自动化浏览器。未安装依赖、重启或结束服务，也未运行会生成仓库文件的构建、内容打包或语音脚本。

## A—G 逐项结论

“未验证（环境阻塞）”表示未取得通过/失败判断所需的实测证据；以下项目都不是游戏缺陷判定。

| 项目 | 结论 | 本轮证据及缺少的验证 |
| --- | --- | --- |
| A. 390×600 下各步主按钮、选项与下一步不越界、不横向溢出 | **未验证（环境阻塞）** | ENV-04—08：未能设置实际视口、读取按钮边界或取得五步画面。不能用样式源码或构建结果替代。 |
| B. 错误选择有明显叉号、红框、文字且可重试 | **未验证（环境阻塞）** | 未点击第一、二、四、五步的错误及正确选项；缺少错误状态与重试后成功状态证据。 |
| C. 第三步拖动能完成，箭头键也能移动 | **未验证（环境阻塞）** | 未进入第三步，未执行鼠标拖动或左右方向键；无法确认接上状态及“去看看关口”按钮。 |
| D. 图片层能打开关闭，有重听按钮 | **未验证（环境阻塞）** | 未打开玉门关图片层，未测试关闭和重听入口；家长折叠区来源链接也未看到。 |
| E. 人物脸部不被道具或标签遮挡 | **未验证（环境阻塞）** | 未取得手机或桌面实际渲染截图；三位小伙伴与工匠相聚图的落位、裁切、遮挡均未检查。 |
| F. 结束页两个按钮去向正确 | **未验证（环境阻塞）** | 未到达结束页，未点击“返回时间河”或“再玩本章”；亲子折叠区、练习区和两个动作也未检查。 |
| G. 浏览器控制台没有红色报错 | **未验证（环境阻塞）** | 未取得页面控制台日志。上面的连接/授权错误属于测试工具，不能算作页面控制台报错；没有读取到日志也不等于零报错。 |

## 用户指定步骤与截图状态

**截图实际生成数：0 / 17。** 没有创建空白、占位或伪造截图。

| 文件名 | 要求画面 | 状态 |
| --- | --- | --- |
| `screens/01-home.png` | 首页时间长河，390×600 | 未取得 |
| `screens/02-station.png` | 秦朝站弹层与两个任务卡 | 未取得 |
| `screens/03-cover.png` | 长城任务封面 | 未取得 |
| `screens/04-step1.png` | 第一步玉门关遗址 | 未取得 |
| `screens/05-step1-wrong.png` | 误选“明 · 1368年”后的叉号与“再试试” | 未取得 |
| `screens/06-step1-right.png` | 重选“秦 · 前221年”后的勾与回应 | 未取得 |
| `screens/07-inspection.png` | 图片层，重听与家长来源入口 | 未取得 |
| `screens/08-step2.png` | 第二步先错后对，选择低矮土石墙段 | 未取得 |
| `screens/09-step3-drag.png` | 第三步拖动前 | 未取得 |
| `screens/10-step3-joined.png` | 第三步接上后 | 未取得 |
| `screens/11-step4.png` | 第四步先错后对，选择走关口过墙 | 未取得 |
| `screens/12-step5.png` | 相聚图与“不一定，年代不一样”正确反馈 | 未取得 |
| `screens/13-finish.png` | 结束页及两个主按钮 | 未取得 |
| `screens/14-family.png` | 展开的亲子练习区和两个动作 | 未取得 |
| `screens/15-back-home.png` | 点击返回后的时间长河 | 未取得 |
| `screens/16-desktop-step1.png` | 桌面1280×800第一步 | 未取得 |
| `screens/17-desktop-step3.png` | 桌面1280×800第三步 | 未取得 |

## 恢复后需要完成的工作

需要先恢复可用的浏览器自动化连接，或在工具界面授权直接控制 Chrome，然后从首页开始完整执行用户给定顺序。鼠标拖动与方向键应分别取得行为证据；“再玩本章”必须实际点击，不能只看到按钮就判定去向通过。来源链接只核对其存在与目标，遵守只使用本机地址的限制，不打开外站。

本轮尺寸设置没有成功执行，因而没有已施加的临时尺寸需要恢复。没有成功取得当前页面 URL 或确认本轮长城改动可见，不构成本机页面验收交付。

## 写入范围

本轮仅新增本文件 `browser-check.md`。已有 `browser-task.md`、`prompts/`、源稿、组件、运行包、音频及其他未提交改动均未写入。未执行 Git 暂存、提交或推送。
