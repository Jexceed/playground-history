# 秦朝长城任务浏览器实测记录（CDP 无头 Chrome）

- 记录时间：2026-09-14（第三轮，看图听故事形态）。
- 页面：`http://127.0.0.1:4173/`（本机 dev server，任务期间曾掉线，已按规范在同端口重启）。
- 尺寸：手机 390×600（deviceScaleFactor 2）；桌面 1280×800。
- 驱动：`cdp-wall-verify.mjs`（Node 24 内置 WebSocket + Chrome DevTools Protocol，无第三方依赖）；截图 17/17 在 `screens/`，逐项断言在 `browser-check-cdp.json`，结果 **25/25 通过**。
- 先期尝试：codex exec 的 Chrome 扩展/Computer Use 通道未获授权，0 截图，如实记录于 `browser-check-codex-attempt.md`。

## 第三轮（看图听故事形态）逐项结论

| 项目 | 结论 | 证据 |
| --- | --- | --- |
| A. 390×600 下各步主按钮与下一步不越界、不横向溢出 | 通过 | 五步 scrollWidth=390=clientWidth；下一步 bottom 436.5—599.9、left/right 在 14—376 内 |
| B. 做题步骤压到最少 | 通过 | 第2、4、5步为 look-listen：DOM 中 0 张选择卡，只有“继续走”大按钮；做题式选择只保留首步朝代定位 |
| C. 故事展开可见可听 | 通过 | 第2步画面含“长城长”故事文字块（桌面/较高窗口显示，小高度手机按既有规则藏文字、由语音承担）；每步自动播 lead+故事，重听按钮可用 |
| D. 第三步拖动能完成，箭头键也能移动 | 通过 | 半途松手触发“还错开一点”；9 次 ArrowLeft 键盘对齐，“墙段接上了”；结束页亲子练习区拖开再复原均验证 |
| E. 图片层能打开关闭、有来源 | 通过 | 首步玉门关图片层 3 条家长来源链接；第四步直接展示玉门关风化照片大图 |
| F. 人物脸部不被遮挡 | 通过 | 12/13/14 截图：三位小伙伴与工匠面部完整 |
| G. 结束页按钮去向 | 通过 | “返回时间河”实测回到时间长河；“再玩本章”在列 |
| H. 控制台无红色报错 | 通过 | 全程 0 条 exception/console.error |

## 未覆盖项

- 语音只验证绑定与不报错；**未人耳试听**（临洮、辽东、蒙恬、万余里等新词待校听）。
- 真实 4—6 岁儿童独立操作未进行；本章不算儿童验收通过。
- 明长城全景与秦长城遗址真实照片待用户下载接入；首步主图届时切换为全景照片。
- 手机小高度窗口按既有规则隐藏故事文字块（语音承担），文字显示只在桌面/较高窗口验证过。

## 历史轮次

- 第一轮（做题形态）23/23、第二轮（内容加深）23/23 均通过；第三轮脚本适配 look-listen 步骤后 25/25。截图以最新一轮为准。
