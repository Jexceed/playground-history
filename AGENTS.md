# 小小历史旅行团协作规范

## 项目与当前认可基线

面向4—6岁儿童与家长共学的历史游戏。儿童层以图像、大按钮、可暂停重播的本地预生成语音为主，不假设孩子识字；长解释、材料出处和编辑审核收在家长独立入口。

用户在2026-09-14认可秦朝样章的整体设计方向，并要求后续任务沿用。2026-09-18又认可汉朝五铢钱章，并要求继续优化张骞与造纸两章；沿用主旨、史料、互动相互对应的方式。本轮已把“尺子示意与砝码照片混作选项”修成同组学习道具示意。2026-09-21依用户恢复指令完成五代十国至清的后续17章；秦至清可见34章现均有本轮新版实现。2026-09-22完成整体内容对齐复核，学习目标、真实材料、可听情境、方法说明与家长页面分别校对。实现或对齐复核完成不等于全库史实终审、真实儿童测试或内容冻结完成。

新任务先读：

1. [当前设计方案](docs/DESIGN-SPEC.md)：3.0认可基线与秦朝五步样例。
2. [任务实施指南](docs/TASK-IMPLEMENTATION-GUIDE.md)：修改入口、复用方式、检查与交付。
3. [改进记录](docs/DESIGN-IMPROVEMENTS.md)：用户反馈、处理方式与回归点。
4. [学习地图](content/learning-map.json)与[整体内容复核](docs/CONTENT-REVIEW-2026-09-22.md)：每章希望理解什么、如何观察理解及全库范围。
5. 相关章节源稿，以及 [角色约定](docs/CHARACTERS.md)、[语音流程](docs/VOICE-WORKFLOW.md)。

不要从旧线上站点、2.0方案或历史反馈段落重新推导当前方向。原2.0 Word稿和 `docs/design/archive/` 只作存档。

## 已确定的产品原则

- 首要目标是让孩子在玩中理解真正的历史。每章同时守住三点：主旨清楚且贯穿开场到收尾；历史图片与文献分别支撑可观察现象和历史解释；互动通过观察、比较、选择或操作帮助理解这条主旨。画面漂亮、操作顺畅或答对了，不单独代表学会历史。
- 问题所需的前提、先后状态与词意必须进入实际播放的入场语音，不能只写在未显示的story里。短引导须自足；不默认拼上冗长正文。家长页应能核对孩子真正听到的内容。
- 制作前写出“核心史实—图片/文献依据—孩子的动作—能观察到的理解”对应关系。每个主要互动都说明学到了什么；不以拼接、串物、找颜色等通用动作替代该章的历史意义，也不把不同年代的事件压成一个时间点。
- 保留原时间长河主页与三位小伙伴带路的故事感。章节围绕一个核心问题，以发现、帮助、回应和分享连接五步，不把每页都写成空洞模板题。
- 核心历史用途必须进入开场、儿童故事和结束回答，不能只藏在家长层。建筑或工程章先讲为什么要建，再讲怎样建；长城必须讲抵御外来进攻、守卫边地，以及城墙、关口与守卫的配合，不能整章只剩修墙与拼接。
- 章节封面与每步图片必须有具体任务语义，不机械复制主页封面。第一步既要展示主题的真实依据，也要完成时间定位；年代可由同屏选项承担。时间定位题还要提供可听到的时代线索，不能只问年代、让不识字的孩子靠小字或盲猜。
- 真实文物提供历史证据，故事插画提供情境，学习道具提供可操作关系。同组选项统一表现类型、画风、比例、背景、标签和清晰度。
- 多角度真实照片可组成可选相册，每张只引导一个观察点，独立标明年代并绑定说明语音和出处。翻图时图片、年代、文字、声音与来源一起更新；相册复用大图入口，不增加关卡或改变完成进度。汉代玉门关可作关防对照，不能当秦墙原貌或风化后的秦墙。
- 年份并立、活字重排、指南针、屋脊与动力等操作复用共享history-lab组件与阶段定义；进入先读完整故事，全部必要阶段完成才前进。动画期间禁重复触发，连杆等几何在中间帧也需一致；学习模型不冒充历史整机或原构造。
- 数学与测量等精确学具由共享几何定义驱动画面和判定，标签必须对应实际边数、刻度与尺寸；不能只换数字或让生成插画承担精确计算。后世印本、文物模型和教学新画明确标注身份；来源日期冲突时核查事件过程与口径，留下页码和说明。
- 普通工具题不能混放真实照片与示意图；选中后桌面使用同款道具。真照片通过证据入口独立显示并可放大、听说明、查出处。
- 儿童主要操作为直接选择或摆放，选错有明确叉号、文字和明显状态，允许重试；选对后给短回应与下一步。主按钮名称必须与实际去向一致。
- 故事页也要按内容安排观察、提问与回应，不能后半章连续只点“继续”。不要求每章都有拖动，不能为了沿用小游戏而挤掉主旨；保留一个拖动环节不等于限制必要问答；图中找人和情境选择可复用共享组件，多问步骤需全部完成才前进，当前问题与语音同步。末轮通过仅标出当前正确对象，不能把所有未选热点一起标绿。
- 真照片找图可以登记原图局部取景，热点使用同一取景坐标，保留完整原图与出处入口。标记与图像身份标签避开观察对象；图卡取景不夹带旁人的半张脸。
- 结束页先显示“返回时间河”“再玩本章”等主行动；复用FinishNavigation，统一图标尺寸、文字居中、按钮高度和投影深度，避免继承首页CTA的特殊布局。完整结束回答和亲子练习放在主导航之后；即使展开，也不能把主导航挤到本轮目标屏幕之外。亲子练习默认折叠，内容必须是具体可执行的动作。
- 黄衣男孩沿用翘短发、无眼镜；绿衣男孩为圆框眼镜、齐刘海；女孩为高马尾和花发夹。角色更新须同步主页、相关场景、结束与分享图，不只靠衣服颜色区分。
- 人物、古诗词、成语和地点要参与叙事或操作；详细文化线索收在家长层。虚构小伙伴不是历史人物，不编造人物亲历记录。
- 每章在learning-map登记学习目标、亲子提问、可观察回应、易混淆处和有说明的相关故事。可以指图或做动作，成人记录提示；不把点击完成、背出年份或填研究表当作理解。主题联系不能暗示没有依据的直接因果。
- 普通大字块不超过90个汉字，第一屏短节点合计不超过120字；这是上限，不是填字目标。
- 不使用伤亡、抢夺、财富、人群高低、文明排名或阵营胜负得分。

## 当前范围与平台边界

以 [BUILD-PLAN](docs/BUILD-PLAN.md)、[TODO](docs/TODO.md) 与生成清单为准：

- 完整库保留7个板块、103章、515步；儿童与家长HTML当前只展示秦至清34章、10站，其余69章源稿和完成记录保留。
- 秦朝样章是已认可的制作基线。后续用户指定章节时直接按此方式推进；本轮不自动批量改其他章节。
- 五步ID统一为 `time / beginning / journey / change / takeaway`，共用同一入口、进度和完成机制。新增操作扩展共享组件，不为单章建立第二入口或状态机。
- 辽、西夏、金与两宋先后并立，不写成宋的下属或简单单线先后。清朝本轮内容截至近代前夜，不表示清朝止于1840年。
- 339个亲子加餐仍属内容库初稿。729篇编辑长稿的1154段正式配音待文字、音色和儿童试听确认。
- 本阶段只做本机网页；内容冻结以前不转换微信、不接登录支付、不部署新版本到公网，不要求孩子登录ChatGPT，不上传儿童数据。
- 用户于2026-09-15明确：当前不做多端适配，但当前指定屏幕尺寸仍是布局与验收依据。用户给出尺寸时按指定值；未给出时读取当前浏览器内容区域的实际宽高并记录，以此作为本轮唯一目标尺寸。保证该尺寸下网页的画面、故事、交互和语音符合产品要求；不自动扩展手机、平板、桌面多套尺寸或断点矩阵。历史QA中的其他设备尺寸不自动成为当前要求。

`research: verified`、初稿完成、自动检查通过、用户认可样章、真实儿童独立完成、正式内容冻结是不同状态，分别记录。

## 本地交付要求

本阶段只返回和打开当前本机预览 `http://127.0.0.1:4173/`。

- 不提供旧ChatGPT Site地址或线上站点卡片；不调用会展示旧线上站点的工具。用户的本机交付要求覆盖技能默认的注册Site展示流程。
- 先确认服务响应和浏览器实际URL，并确认一个本轮变化确实可见。只看到旧页面或仅构建成功不能交付。
- 优先复用本机服务和正确标签。不要静默换端口、误用自动化内部标签句柄，或把工具返回“queued”说成用户已经看到页面。
- 实际启动说明在 `本机试玩说明.md`，可用 `本机试玩.command`。需要恢复服务时先检查已有进程，不终止无关服务。

## 事实来源层级

从高到低：

1. 教育部课程标准、统编教材与官方教学说明。
2. 中国国家级和省级博物馆、考古研究机构、档案馆、政府与权威党史资料。
3. 对应地区博物馆、国家图书馆、档案馆、大学馆藏与UNESCO。
4. 开放授权文件页，仅用于照片作者、拍摄与馆藏标识、许可等元数据；事实仍由权威资料支持。
5. 商业媒体、旅游文章、百科整理仅帮助发现线索，不直接支撑儿童正文。

中国史保持中国课程与材料脉络，世界史保持独立时间线，不虚构同期关系。事实、推断、文学表达与教学虚构分开；文物不能代替真人自述，复原不能冒充现场，一张图不能推出人物动机或全国状况。

事实卡记录可核实的历史与材料信息，在句末引用已登记来源ID。史料阅读方法、活动数量、表格填写规则等另列“学习设计与史料阅读方法”，不计入历史事实数；课程标准作为设计参考，不冒称其中规定了本项目的具体操作。成人整理复杂记录，儿童不承担多格填表和固定句式背诵。史实/器物身份与照片许可分别核验；开放授权参观照片不冒称博物馆官方摄影。年代不同的对照材料直接标注，例如“汉代，秦之后”。找不到可靠秦尺照片就保留缺口，不能改标汉尺或生成假史料。

## 文件权威与生成边界

按以下顺序判断当前内容：

1. 根 `AGENTS.md`：长期原则、范围、质量门禁。
2. `docs/DESIGN-SPEC.md`、`docs/BUILD-PLAN.md`、`docs/TODO.md`：设计基线、阶段与任务。
3. `content/product-map.json`、`content/catalog*.json`：完整范围；`content/preview-scope.json`：当前展示范围。
4. `content/focused-quests.json`：当前34章试玩讲述、问题、选项、entry材料/地点/文化信息、表现配置与结束动作；`content/learning-map.json`：学习目标、明确选取的词语与章节联系；`content/chapters/*.md`：研究事实与编辑稿。
5. `content/sources.json`、`content/assets/*.json`、`reference-assets/*/ATTRIBUTION.md`：来源、许可与边界。
6. `public/content/**`、`content/runtime/**`、声音清单：脚本生成的运行包，禁止手改。

历史站的开场、时间释义与选材范围统一取`content/catalog.json`的`periods[].learningContext`；展示短日期可用`stationYears`，不在每章重复散写站介绍。家长页用实际ObjectView与互动类型，不能把操作判定项显示成二选一，也不能把旧长稿的屏数、音稿数当当前五步。

`prepare-focused-quests.mjs`将重点章逐题稿投影到低龄入口、故事拍点与课本连接；先改正式源稿。其余69章仍保留原章节与入口索引，未完成本轮内容重做。

进度数字取自 `content/PROGRESS.md` 与生成后的manifest，不能复制旧报告数字当现状。

## 素材与声音

- 来源日期按“事件、编纂、刻制、刊印、重建、拍摄”分别记录。文献页面有冲突时写清采用的段落/页码与口径，不能把后世刊本、重建形制或三语不同内容说成同一时点/逐字翻译。
- 当前34章的难词按learning-map明确登记，不能用任意子串匹配把“独立入口”解释成国家独立。当前校听名单与旧编辑稿校听范围分开，保留仍有出处的全库词条。
- 家长可见的素材boundary写身份、证据范围与许可；原始像素、下载故障、提示词路径等编辑细节留在notes和QA。
- 素材身份、来源、许可、用途与边界登记在 `content/assets/*.json`。原图在 `reference-assets/`；运行缩图在 `public/`，只用已清权材料或明确自制图。
- 未确认许可的素材只供编辑核对，不进入儿童层；低清史料不放大要求儿童辨字。原图不进入普通Git，需要跨机器同步时另行设计LFS或对象存储。
- 新图先看参考，记录局部编辑目标与不变量；保留旧源图和实际提示词，项目引用不能只留在工具默认目录。
- 当前游戏复用edge-tts脚本，联网合成后本机播放；F5-TTS是本机另有的方案。不得把二者混称，不每轮重写生成器或擅自切换声音引擎。
- 图片/布局修改不重生成声音；修改台词或读音后用 `npm run voice:update` 增量更新。`量一量`按 liáng yī liáng 校音，不能影响“度量衡”。
- 页面介绍、短引导、提问、选项、正误回应、图片说明和结束回答分别绑定稳定声音ID。优先复用 `stepVoiceIds`，避免重复拼接长语音。阅读页 `look-listen`应播放 `audio.intro`完整故事，一次继续即前进；普通题目与操作仍需完成后继续。
- 音频暂存后统一替换；断网或合成失败不得损坏已有MP3。生成清单不手改，自动匹配不等于人工校听通过。

## 修改、验证与记录

1. 先明确本轮范围，查看现有实现与源稿，保护已有未提交改动。已确定的设计不反复请求确认。
2. 修改源稿、来源/素材登记和共享组件；题干、图形、被选中的场景对象、声音与反馈同步。
3. 运行 `npm run content:validate:assets`、`npm run content:pack`、`npm test`、`npm run lint`、`npm run typecheck:app`、`git diff --check`。`npm test`包含构建；新改动完成后做必要检查，不无故反复全跑。
4. `content:audit`检查全库103章/515步，含同组选项技术呈现一致性。它不能替代画风、语义、出处和儿童理解的人工检查。
5. 在本轮目标尺寸下检查实际网页并记录宽高：图文与声音对应、人物和操作物清楚，主按钮不越界、被裁切或遮挡，页面不意外横向溢出。自动巡检须在答题反馈后、下一次自动点击或滚动前检查按钮完整边界与遮挡；失败应返回非零退出码，不能把自动滚动后的通关当作首屏可见。按改动实测选择、重试、拖动替代和结束按钮去向。需要复现时可指定该尺寸；不追加多端测试。临时调整检查窗口后恢复原状。
6. 保留QA记录，说明实际验证与未覆盖部分。仅改素材不必重跑所有关卡；涉及核心操作要实测该操作。
7. 更新 `PROGRESS.md`、`TODO.md`，用户可见变化更新 `CHANGELOG.md`。设计变化更新方案和改进记录；长期约束归入本文件。

系统性问题必须检查完整103章的结构影响；隐藏69章的内容问题继续待办，不能因隐藏视为已修完。图片或外观修订不应无故改变内容版本、清空完成记录或重置可兼容的继续进度。

不得自动推送；只有用户要求才做本地里程碑提交。提交前核对暂存清单，不覆盖、暂存或提交无法确认归属的改动，不包含研究原图、缓存、构建目录和密钥。
